# Raw MAF comparison placeholder

This example intentionally waits for the real Microsoft Agent Framework package/API signatures.

The comparison must be written honestly:

- same behavior as `Motif.fs`
- no artificially verbose raw MAF code
- count only user-facing code, excluding blank lines/comments
- if raw MAF is almost as concise, Motif fails this case
